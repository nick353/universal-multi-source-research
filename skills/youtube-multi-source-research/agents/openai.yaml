interface:
  display_name: "Universal Multi-Source Research"
  short_description: "テーマ・URLから全媒体を横断調査"
  default_prompt: "Use $youtube-multi-source-research to research this topic or URL across every relevant configured source, report any first-use setup needed, and return a cited brief."
policy:
  allow_implicit_invocation: true
