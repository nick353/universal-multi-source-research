interface:
  display_name: "Universal Multi-Source Research"
  short_description: "Cross-platform research from topics and URLs"
  default_prompt: "Use $youtube-multi-source-research to automatically choose the research depth, perform live read-only retrieval across every relevant configured source including X and Reddit, validate source-native evidence before claiming completion, put coverage and counts first, show script-ready YouTube transcript materials, save the cited brief locally, and report any setup or blocker."
policy:
  allow_implicit_invocation: true
